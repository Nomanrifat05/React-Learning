// onChange = event handler used primarily with form elements like <input>, <textarea>, <radio>, and <select> to handle user input dynamically. It is triggered whenever the value of the element changes, allowing you to update the state or perform actions based on user input.
//            Triggers a function every time the value of the input changes, not just when the input loses focus or is submitted.


import MyComponent from "./MyComponent"
function App() {

  return (
    <>
    <MyComponent />
    </>
  )
}

export default App
